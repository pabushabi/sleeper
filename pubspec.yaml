name: sleeper_timer
description: An app that close apps/turn off cellular then you fall asleep.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: 3.22.2
#  scoped_model: 1.1.0
  system_shortcuts: 1.0.0

  cupertino_icons: ^1.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
